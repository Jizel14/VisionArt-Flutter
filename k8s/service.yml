apiVersion: v1
kind: Service
metadata:
  name: flutter-service
  namespace: visionart
spec:
  selector:
    app: visionart-flutter
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30001
  type: NodePort
